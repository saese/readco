<% @subtopic.links.each do |link| %>
          <!--/stories-->
          <div class="row">    
            <div class="col-sm-10">
              <h3><%= link.title%></h3>
              <p><%= link.url %></p>

              <small class="text-muted"><%="#{time_ago_in_words(link.created_at)} ago"%> •<a href="#" class="text-muted">Read More</a></small>
              </h4>
            </div>
          </div>
          
          <div class="row divider">    
             <div class="col-sm-12"><hr></div>
          </div>
          <% end %>